import { Route } from "@angular/router";
import { PopoverExampleComponent } from "./pagination-example.component";

export default [
    {
        path: '',
        component: PopoverExampleComponent,
    }
] satisfies Route[];
